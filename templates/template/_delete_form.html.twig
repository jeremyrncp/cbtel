<form method="post" action="{{ path('app_template_delete', {'id': template.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cette entité ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ template.id) }}">
    <button class="btn btn-secondary">Delete</button>
</form>
