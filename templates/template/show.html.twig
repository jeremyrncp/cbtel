{% extends 'base.html.twig' %}

{% block title %}Template{% endblock %}

{% block body %}
    <h1>Template</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ template.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ template.type }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ template.content|raw }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_template_edit', {'id': template.id}) }}">Editer</a>
    <br />
    {{ include('template/_delete_form.html.twig') }}
    <br />
    <a href="{{ path('app_template_index') }}">retourner à la liste</a>

{% endblock %}
