{% extends 'base.html.twig' %}

{% block title %}UserCampaign{% endblock %}

{% block body %}
    <h1>UserCampaign</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_campaign.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_campaign.createdAt ? user_campaign.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_campaign_index') }}">back to list</a>

    <a href="{{ path('app_user_campaign_edit', {'id': user_campaign.id}) }}">edit</a>

    {{ include('user_campaign/_delete_form.html.twig') }}
{% endblock %}
