{% extends 'base.html.twig' %}

{% block title %}DefaultTemplate{% endblock %}

{% block body %}
    <h1>DefaultTemplate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Type</th>
                <td>{{ default_template.type }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ default_template.template.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_default_template_edit', {'id': default_template.id}) }}">Editer</a>
<br />
    {{ include('default_template/_delete_form.html.twig') }}
    <br />
    <a href="{{ path('app_default_template_index') }}">retourner à la liste</a>

{% endblock %}
